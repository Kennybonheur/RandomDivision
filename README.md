# RandomDivision
"This program generates an array of numbers using a built-in function that produces random values. 
These values are stored and displayed in the array. In addition,
the program performs a division operation where each number is divided by the next element in the array."
